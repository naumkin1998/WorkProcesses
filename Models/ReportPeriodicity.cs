namespace WorkProcesses.Models
{
    public enum ReportPeriodicity
    {
        None,       // без периодичности (разовое)
        Daily,      // ежедневно
        Weekly,     // еженедельно
        Monthly,    // ежемесячно
        Permanent   // постоянное (без срока)
    }
}